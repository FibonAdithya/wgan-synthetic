# Pinned so that a green CI run means "passed against these versions" rather
# than "passed against whatever resolved today". Bump deliberately, in a commit
# that says why.
numpy==2.5.1
torch==2.13.0
pyyaml==6.0.3
tqdm==4.68.4
scikit-learn==1.9.0
scipy==1.18.0
# HDF5 reader for the ann-benchmarks mirrors, used only by src/data/fetch.py.
h5py==3.16.0
# Parquet reader for the one family ann-benchmarks generates rather than
# hosts: openai comes from a HuggingFace dataset's shards, not an HDF5
# mirror. Also only used by src/data/fetch.py.
pyarrow==25.0.0
plotly==6.9.0
# Static PNG export in src/eval/eda_report.py. Kaleido v1 drives a headless
# Chrome; run `plotly_get_chrome` once if no browser is installed. The HTML
# report is produced without it -- pass --no-png to skip export entirely.
kaleido==1.3.0
# Bitmap drawing for src/eval/plot_distance_cdf_pillow.py and
# src/eval/plot_embedding_clusters.py. Both already imported it; it was simply
# missing here, so on a clean install those two modules raised ImportError.
# Note what is deliberately still absent: matplotlib. The glyph-grid plan
# records that decision, and src/eval/plot_distance_cdf.py -- the one module
# that imports it -- draws the same figure as the Pillow version above.
pillow==12.3.0
# Test suite. Run from the repo root:
#   python3 -m pytest
# or `make check` to run lint and formatting alongside it.
pytest==9.1.1
# Lint and formatter. Configured in pyproject.toml; `make check` gates on it.
ruff==0.14.5
